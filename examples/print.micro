fn fibonacci(n) {
    if (n != 0) {
        let result = fibonacci(n - 1) + n
        println(n, ": ", result)
        return result
    } else {
        return 1
    }
}

let result = fibonacci(5)

print("result: ", result)
